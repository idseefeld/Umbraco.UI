import { defineElement } from '@umbraco-ui/uui-base';

import { UUITabElement, UUITabGroupElement } from '../lib';

defineElement('uui-tab', UUITabElement);
defineElement('uui-tab-group', UUITabGroupElement);
